import React, { createContext, useContext } from 'react'
import { cartContext, useCartContext } from '../../store/cart-context'

// const {productList} = useCartContext();
// const {addToCart} = useCartContext();

export function otraCart() {
  return (
    <div>otraCart</div>
  )
}
